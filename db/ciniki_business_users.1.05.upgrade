#
# Add the uuid column to ciniki_business_users for replication.
#
ALTER TABLE ciniki_business_users ADD COLUMN uuid char(36) not null after id;
UPDATE ciniki_business_users SET uuid = uuid();
ALTER TABLE ciniki_business_users COMMENT='v1.05';
