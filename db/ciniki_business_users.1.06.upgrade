#
# Add the uuid column to ciniki_business_users for replication.
#
ALTER TABLE ciniki_business_users ADD COLUMN eid varchar(50) not null AFTER user_id;
ALTER TABLE ciniki_business_users COMMENT='v1.06';
